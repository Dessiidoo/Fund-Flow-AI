## Packages
recharts | For dashboard analytics and campaign performance charts
framer-motion | Smooth animations for route transitions and list items
lucide-react | Beautiful icons for the interface (already in stack but confirming usage)
date-fns | Formatting dates for timestamps and activity logs

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
